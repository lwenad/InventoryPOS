workflow: GitHubFlow/v1
tag-prefix: '[vV]'

branches:
  main:
    increment: Patch
    is-main-branch: true
    
  feature:
    increment: Minor
    pre-release-weight: 10000
    
  pull-request:
    increment: Patch
    pre-release-weight: 30000
